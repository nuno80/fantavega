Subject: [PATCH] fix(authz): enforce admin role in server actions

Authentication alone is not authorization. Apply the existing checkIsAdmin
helper before any query or mutation in these actions.

--- a/src/lib/actions/league.actions.ts
+++ b/src/lib/actions/league.actions.ts
@@
 export async function updateTeamNameAction(...) {
- const { userId: adminUserId } = await auth();
+ const { userId: adminUserId, sessionClaims } = await auth();
  if (!adminUserId) {
@@
  }
+ if (!(await checkIsAdmin(adminUserId, sessionClaims as Record<string, unknown> | null))) {
+   return { success: false, message: "Solo gli admin possono eseguire questa operazione." };
+ }
@@
 export async function updateLeagueStatusAction(...) {
- const { userId: adminUserId } = await auth();
+ const { userId: adminUserId, sessionClaims } = await auth();
@@
  }
+ if (!(await checkIsAdmin(adminUserId, sessionClaims as Record<string, unknown> | null))) {
+   return { success: false, message: "Solo gli admin possono eseguire questa operazione." };
+ }
@@
 export async function updateActiveRolesAction(...) {
- const { userId: adminUserId } = await auth();
+ const { userId: adminUserId, sessionClaims } = await auth();
@@
  }
+ if (!(await checkIsAdmin(adminUserId, sessionClaims as Record<string, unknown> | null))) {
+   return { success: false, message: "Solo gli admin possono eseguire questa operazione." };
+ }
