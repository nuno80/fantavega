Subject: [PATCH] test(auction): cover viewed-only and admin invariants

Add behavior tests instead of relying only on static legacy-call guards.

--- /dev/null
+++ b/tests/e2e/viewed-only-regressions.e2e.test.ts
@@
+it("does not activate league B while polling league A", async () => {
+  // Seed one pending timer per league for the same user.
+  // GET league A state.
+  // Assert both deadlines remain null.
+  // POST viewed for league A/player A.
+  // Assert only timer A has a deadline.
+});
+
+it("claims a viewed timer once across two tabs", async () => {
+  const results = await Promise.all([postViewed(), postViewed()]);
+  expect(results.filter((r) => r.status === "activated")).toHaveLength(1);
+  expect(await countTimerStartedEvents()).toBe(1);
+});
+
+it("rejects non-admin server actions before a write", async () => {
+  await expect(updateLeagueStatusAction(nonAdminState, form)).resolves.toMatchObject({ success: false });
+  expect(writeQueries()).toHaveLength(0);
+});
*** End Patch
