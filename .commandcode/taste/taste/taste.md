# Taste
- Communicates in Italian; expects replies in Italian. Confidence: 0.95
- Prefers to be guided step-by-step through deployment/platform procedures ("puoi guidarmi?"): asked how to verify a platform-supplied value himself ("come faccio a capire se site--fantavega--...code.run è corretto?") rather than accept the agent's verification on faith — expects the checks spelled out so he can confirm and trust the config (Socket.IO handshake endpoint in the browser, cross-check against Northflank Domains/Public URL tab). When told to copy a key value from an external dashboard, he asks where exactly ("non ho capito il punto 2 da dove copio il codice?") — instructions must give the full click-path (Clerk Dashboard → project → API Keys) plus how to recognize the right value (publishable key starts with `pk_`, secret with `sk_`), not an abstract reference. Confidence: 0.85
- For web-UI/platform steps, wants the assistant to hand over the exact ready-to-paste content as a verbatim code block ("incolla TU PER ME") so they can copy it as-is into GitHub/web forms, instead of being told what to write themselves. Confirmed again when, unsure of the value for the Northflank `ALLOWED_ORIGINS` env var, he asked tersely "allowed_origin cosa devo mettere?" — expecting the exact string (plus formatting constraints like no trailing slash, comma-separated for multiple origins) to paste into the form. Confidence: 0.65
- Prefers creating/editing repo files via the GitHub web UI (Add file → Create new file, commit directly to main) instead of using local git operations. Confidence: 0.6
- When migrating a project to a new platform, wants the new deployment config to be an exact replica of the previous working setup (e.g., Railway's Node 20, prod-only deps, no Next.js build) rather than a redesign; on a new platform (Northflank) he again pasted the Railway-style Dockerfile (including `pnpm install --prod`) and asked to confirm it, showing he expects the new platform to accept the old working config as-is. Confidence: 0.7
- Delegates hands-on repo/git work to the agent ("fallo tu per me", "fallo tu"): after agreeing on the plan, expects the agent to execute it end-to-end — create/edit files locally, commit, merge branches into main, push, restore the working branch — not just hand over instructions or paste-ready content for the user to apply. Confirmed again when the user replied "fallo tu" to the offer to merge/push the branch into main, and again with a one-word "si procedi" approving the final merge/squash/branch-cleanup sequence. Confidence: 0.95
- Approves merging a feature-branch PR with squash into a single atomic commit on main (with local+remote branch deletion and local main ff-synced), when the issue plan calls for one commit per issue — accepted the assistant's squash-merge offer with one-word approval and didn't object to branch deletion. Confidence: 0.55
- Considers the main branch's commit history fundamental: after a merge/push he asked whether main's commits were kept ("hai mantenuto i commit di main? erano fondamentali"), showing he expects git operations to be non-destructive (no history rewriting/force-push) and wants explicit confirmation that no commit was lost. Confidence: 0.7
- When asked to verify/test functionality, prefers the agent to create and run a complete automated test suite (unit + integration) covering the feature's behaviors rather than guiding them through manual/click-through procedures; expects the suite to be actually executed, failures fixed, and results reported. Confidence: 0.7
- Before executing fixes for failing tests/bugs, wants the agent to analyze the root cause and propose solution options first, then wait for approval ("fai una analisi ... e dimmi che soluzioni proponi prima di eseguire le correzioni"); expects options with a recommendation and a go-ahead before any code change. The same plan-first rule extends to implementation work: he explicitly asked for a detailed action plan including a risk assessment of possible breakage of existing app logic before any code is written ("prima proponi un piano di azione dettagliato con una valutazione del rischio di una eventuale rottura della logica dell app"). Confirmed again when the user approved the recommended fix with a one-word "si" and only then let the agent apply it. Reinforced for destructive data operations too: before allowing the full-catalog-replacement import, he demanded to reason through the edge cases first ("non scrivere ancora codice prima ragioniamo") — asking what happens to players who changed team and to active leagues affected by the destructive change — and expects the assistant to analyze the consequences (and flag the domain decision needed) before writing any code. Confirmed again when the user approved a commit-review payload-mismatch analysis with "si e poi proponi un piano di azione" — explicitly asking for a structured action plan (test-first, backend fix, client fix, fallback, cleanup, plus what not to do) to be proposed before any code is written. Confidence: 0.99
- Does not take GitHub issue/PR status at face value ("Closed", "merged"): asks for a codebase audit against the issue's actual requirements to determine what is really implemented and what still needs to be done. Confidence: 0.6
- Before deploying, wants confirmation that main, Vercel and the Socket.IO server actually point to the same merges (deployment-drift check) — otherwise production may keep running old code even though the fixes are merged. Confidence: 0.7
- When the user proposes corrections/refinements to the agent's plan, wants them critically evaluated against the real codebase before being incorporated — explicitly asked "valutale prima attentamente" and only then "modifica il piano ... se sono corrette"; the agent must not blindly agree or blindly apply. Confirmed again when the user proposed his own two-button design ("update players DB" + "reset players DB") and asked "Cosa ne pensi?" — expecting the agent to critically evaluate and refine the design (e.g., reset must also clear rosters) before implementing. Confirmed once more when the user proposed a full code-level fix for the socket auth failure (replace `clerkClient().authenticateRequest()` on a fabricated Request with Clerk's `verifyToken`, relying only on `CLERK_SECRET_KEY`) and asked "cosa ne pensi? è una possibile soluzione?" — the agent evaluated it against the real codebase (verified `verifyToken` is re-exported by `@clerk/nextjs/server`, no new dependency needed) before implementing, updating the test mock, and pushing. Confidence: 0.95
- Wants an implementation plan finalized into a written plan document and explicitly saved to a file (e.g., `~/.commandcode/plans/agosto.md`) before work starts; the saved plan should incorporate all agreed refinements and be framed as a single atomic PR. Confidence: 0.7
- Wants tests to assert real observable state rather than only mock-call assertions: corrected the plan so the HTTP-polling test verifies the DB session stays open (recordUserLogout called but updating zero rows thanks to the heartbeat guard), not merely that logout was never invoked. Confidence: 0.7
- For DB migrations, wants data sanitization before adding new constraints: close duplicate open sessions first, otherwise `CREATE UNIQUE INDEX` on dirty data fails; the cleanup must be non-destructive (UPDATE `session_end` preserving history — e.g., `COALESCE(last_heartbeat, session_start)` — not DELETE) and the index creation must run in the same transaction as the cleanup. Transaction control (BEGIN/COMMIT/ROLLBACK) belongs in the migration runner, not embedded in SQL files (the runner may split statements), and migrations need tracking (e.g., a `schema_migrations` table) so they are not re-applied at every startup. Confidence: 0.9
- Wants per-instance server state scoped in the closure of a single server instance rather than module-global state: globals make tests and restarts within the same process share state and race. Confidence: 0.8
- Wants precise TypeScript typing for timers: uses `ReturnType<typeof setTimeout>` for timeout handles instead of a hardcoded type that may mismatch Node/browser typings. Confidence: 0.7
- Wants stale-async-callback protection: callbacks must only mutate shared state if their handle/token is still the current one (e.g., `disconnectTimers.get(userId) === timer` before `delete`), so an obsolete callback can't cancel a newer timer. Confidence: 0.8
- Wants DB write paths handling unique-index races (e.g., concurrent heartbeats both doing UPDATE→0 rows then racing the INSERT): expects an INSERT retry that falls back to repeating the UPDATE on unique-conflict error, rather than letting a concurrent write fail. Confidence: 0.7
- Wants tests separated by layer rather than mixing mocks and real DB in one suite: socket logic tested with fake timers + mocked service, DB behavior tested against a real in-memory engine (libSQL `:memory:`), without half-mocking the DB. Confirmed again in the player-import regression suite, which runs against a real in-memory libSQL DB with `fs`-loaded schema and real FK fixtures (admin → league → manager) rather than mocked DB behavior. Confidence: 0.85
- Requests explicit guardrail tests for lifecycle edge cases before opening a PR: a test that `close()` clears pending timers (no leaked timers/state), and a test for reconnect arriving after the timer has already fired. Confidence: 0.8
- For destructive/irreversible operations, wants safety-guard tests proving the operation refuses to run on invalid input — e.g., a replace-mode catalog reset must not delete anything when the uploaded file is empty/invalid (the test asserts players and roster assignments are untouched). Confidence: 0.8
- Prefers fake timers (`vi.useFakeTimers` + advance) to real sleeps in tests; if fake timers prove fragile with socket.io internals, accepts a configurable shortened real timeout instead. Confidence: 0.6
- Wants implementation plan documents turned into actionable checkbox task lists ("trasforma il file in una task list utile per completare l'implementazione"): per-task checkboxes organized by phase, each carrying the essential technical detail needed to execute it, plus a final verification checklist and a risk/mitigation table — prose plans are not enough, the document must be directly executable task by task. Confidence: 0.7
- Drives implementation through the numbered checklist of the plan document: refers to tasks by their number/phase ("ok procedi con il punto 1", "procedi con la fase B", "completa la fase E e i relativi test") rather than re-describing them, and grants go-ahead per point — the agent is expected to track the checklist, execute the approved phase/point including its tests, verify (typecheck + existing tests) and report completion. Confidence: 0.85
- Keeps the plan document up to date as work progresses: after finishing phases he asks to mark the completed checkboxes ✅, update the header status with the commit reference, and keep the task list aligned with what was actually done ("marca le fasi completate"). The plan file is the living source of truth for progress. Confidence: 0.8
- Wants debugging lessons learned codified so the same mistakes don't recur: asked for a detailed testing guide to be embedded in the plan file (commands, environment rules, mandatory patterns, anti-patterns seen during debugging) explicitly so "non si ripetano gli stessi problemi nei test". Confidence: 0.8
- Wants hermetic, deterministic socket/integration tests: never bare `client.close()` (server-side disconnect is async — sockets become phantom and pollute later tests); wait until the server observes the disconnect (poll server socket count); use a short injectable `disconnectTimeoutMs` instead of real long waits; and register `vi.mock` under the exact same module specifier/alias (`@/...`) the server imports, or the mock won't intercept. Confidence: 0.6
- Fantacalcio "listone" domain rule the user wants respected in code: player IDs are frozen for the entire season — mid-season (January) updates keep the same ID for team changes, append new IDs only for brand-new signings, and never reuse an old ID for another player; at season change the new file carries all-new IDs and the previous season's leagues are already closed ("è tutto già chiuso"). Import logic must therefore distinguish these two scenarios (mid-season upsert is already correct; season change needs a guarded full-catalog replacement) rather than one blanket destructive replace-everything option. Confirmed again when the user asked for exactly two separate buttons — "update players DB" (mid-season, current logic preserved) and "reset players DB" (season start) — instead of a single replace action. Confidence: 0.95
- Defensive about production data: questioned a shell command aimed at the production Turso DB ("a cosa serve questo comando distruttivo?") even though it was read-only — expects the agent to explain in advance what a command does and why it is safe before running anything against production, and to prefer read-only verification there. Confidence: 0.7
- Security-conscious about leaked secrets: when he noticed that real values of `CLERK_SECRET_KEY`, `TURSO_AUTH_TOKEN` and `SOCKET_EMIT_SECRET` appeared in shared screenshots, he proposed rotating them as a precaution ("io li ruoterei prima di continuare") — the agent should treat credentials exposed in pasted screenshots/logs as compromised and recommend rotation. Confidence: 0.7
- At the end of an issue, expects a definitive completion confirmation ("resta altro da fare o abbiamo completato tutto il lavoro e i test?"): the agent must verify the plan checklist is fully checked off, all test suites pass, and the PR/CI state is actually green (including distinguishing a pending check like Vercel Preview from a real failure) before declaring the work done — a verbal "done" alone is not enough. Confidence: 0.6
- Uses Turso (remote libSQL over HTTP/WS) as their only database in production ("perchè uso solo quello"): DB code, migrations and tests must be portable to Turso remote, not just work on local file/`:memory:` DBs — when discussing DB behavior, explicitly check that it also holds on Turso. Confidence: 0.9
- Uses (and tests) an inactivity auto-logout hook: after 30 seconds without input the app calls `POST /api/user/set-inactive` and redirects — the recurring `set-inactive` calls in the logs are expected when the page sits idle, and the agent should distinguish that from a bug where the timer fails to reset during active use; he is open to having the aggressive `timeoutSeconds` raised to a more humane value (e.g., 5–10 minutes) on request. Confidence: 0.6
- Reports UI-observed anomalies as suspected bugs and expects the agent to independently verify them against the codebase/DB before concluding or fixing ("forse ho trovato un nuovo bug ma devi verificare"): the observation is a hypothesis to investigate (e.g., check whether the form actually serializes the field into FormData), not a diagnosis to take at face value. Confirmed again when he reported the status-change bug as persisting after the fix ("Permane il bug per il cabio di stato. Puoi controllare...") — the agent re-verified the DB, the deployed build, and the action chain rather than assuming the previous fix resolved it. Confidence: 0.8
- For atomic transactions with the libSQL client, prefers `client.batch([...], "write")` — the official portable API (file + Turso HTTP/WS) — over manual `BEGIN`/`COMMIT` via `client.execute()` (on Turso HTTP each execute() is a separate stateless request, so transaction state isn't guaranteed) and over `client.transaction()` (opens a separate logical connection, verified useless on `:memory:`). Confidence: 0.8
- When debugging UI bugs on the deployed app, expects the agent to verify the actual production database state directly (read-only queries against the Turso DB, checking the live DDL, triggers and row values) to distinguish "backend works but UI sends wrong data" from "DB constraint blocks the change" — the agent ran read-only probes plus a reversible UPDATE on league 16 (change → check → restore immediately) to prove the DB and service layer accept the update and the fault lies in the UI/deploy. Confidence: 0.7
- When a UI submit must carry a value from a non-native control (Radix Select), expects defense-in-depth: the hidden input stays in sync with the selected state, AND the submit handler explicitly overwrites the FormData field with the current selected value before passing it to the action, so a desynced React state can't cause the form to send a stale value. Confidence: 0.7
- Wants detailed, well-structured git commits (explicitly "fai un commit dettagliato e un push"): expects a conventional-commit-style message that explains the context/why, the list of changes per file, and the verification performed (tests green + typecheck), and then the branch pushed to origin — not a one-line summary left un-pushed. Confirmed again verbatim ("fai un commit dettagliato e un push cosi si aggiorna anche vercel e testo") and again with "fai un commit e un push in modo da aggiornare anche vercel", and once more with a terse "fai un commit e un push" (the agent staged only the fix file, wrote a context/why conventional-commit message, pushed to main, and reported the commit hash). Confirmed again verbatim with "ora fai un commit dettagliato e un push" — the agent staged only the relevant files, wrote a per-fix detail message, pushed to origin/main, and reported the commit hash plus what was left out. Confidence: 0.97
- Wants the final commit/push to main gated on the full test suite passing: instructed "applica le correzioni necessarie mancanti fai i test e se passano facciamo un commit dettagliato e un push su main" — run the tests first and commit/push only when green, which includes fixing test-environment/config issues to make the whole suite pass (e.g., e2e tests failing in jsdom because vitest externalizes node:crypto/child_process were fixed by mapping tests/e2e/** to the node environment in vitest environmentMatchGlobs) rather than committing with known failures, excluding tests, or pushing a partial green subset. Confidence: 0.7
- Uses push-to-main as the deployment trigger for Vercel (auto-deploy from the push) and then tests the deployed app himself; when asking for a commit he frames it as "cosi si aggiorna anche vercel e testo" — the agent should commit, push, and report the deploy trigger + a checklist for testing the live/preview environment, rather than treating the push as the end of the work. Confirmed again: right after the fix deploy he replied "ok funziona" and immediately moved to the next production bug, showing he personally verifies each deployed fix before reporting new issues. Confirmed again when he asked "fai un commit e un push cosi verifico se funziona su vervel" — the commit-and-push request is explicitly framed as triggering a Vercel deploy for his own verification, and the agent is expected to report the commit hash plus the conditions under which the expected behavior holds (socket emission path + fallback). Confirmed again when he checked in with "hai fatto il push per aggiornare vercel?" — he tracks the push→Vercel deploy pipeline and expects confirmation that the push landed (commit hash, branch) plus an explanation of how the deploy is triggered (auto-deploy vs manual) rather than the push being the silent end of the work. Confirmed once more when, after correcting `NEXT_PUBLIC_SOCKET_URL` himself on the Vercel dashboard, he asked "fai un commit per aggiornare vercl" — he assumes a commit/push is the mechanism that propagates any fix to Vercel, so the agent must first determine where the change actually lives (repo vs dashboard-managed env var, `.env.local` being gitignored) and explain when a commit cannot help: dashboard-only env changes need a Vercel redeploy, not a commit. Confidence: 0.95
- When asked to commit, expects the agent to first inspect `git status` and commit only the files belonging to the fix, deliberately leaving unrelated working-tree changes (e.g., `.commandcode/`, scratch notes like `agosto.md`) untouched and unstaged — and to say so explicitly in the report. Confidence: 0.5
- Reports production bugs by pasting raw output verbatim rather than describing the symptom in his own words — Vercel deployment logs (Request ID, path, host, referer, search params, middleware trace, deployment ID), browser error pages (e.g., the Chrome `ERR_CONNECTION_REFUSED (-102)` page with the exact failing URL `http://localhost:3001/`), and full Northflank service logs (timestamped stdout/stderr lines, e.g., the `[DEBUG-SOCKET-SRV] auth error: Publishable key is missing...` block) with no accompanying explanation; he expects the agent to diagnose the root cause from the pasted output (e.g., the `/api/players` 404 traced to Clerk middleware route lists; the `localhost:3001` fallback traced to a missing `NEXT_PUBLIC_SOCKET_URL` in the build; the missing `CLERK_PUBLISHABLE_KEY` env var inferred straight from the pasted auth error) instead of asking him for more details. The same verbatim-paste habit applies when he supplies requested deployment/config details: asked for the socket server URL, he pasted the exact Northflank public URL (`site--fantavega--h5y5f687z4v2.code.run`) with a terse confirming question, expecting the agent to recognize and verify it (handshake/CORS/emit probes) rather than re-ask. Confirmed again when, after adding the `CLERK_PUBLISHABLE_KEY` variable on Northflank, he tersely reported "fatto ma" and pasted the raw log block still showing `auth error: Publishable key is missing` — the agent had to infer from the log timestamps (pre-restart) that the service hadn't been restarted with the new variable yet, rather than ask him for more details. Confirmed again when he pasted the follow-up raw log block (16:34:07, still showing `auth error: Publishable key is missing`) with no text at all — the agent inferred the next diagnostic step is checking the exact saved variable name/value/service placement on the platform, not asking him for details. Confirmed again when, right after the verifyToken fix deploy, he pasted the Socket Debug overlay state verbatim ("Socket Events × Connected, URL: https://site--fantavega--h5y5f687z4v2.code.run:443/socket.io, No events yet...") with no explanation at all — the agent read it as the connection now succeeding and moved straight to the functional test (make a bid, watch the overlay) rather than asking for more details. Confirmed again when, after the socket flow went live, he pasted the overlay's `auction-created` event payload verbatim (full JSON: playerId, auctionId, newPrice, highestBidderId, scheduledEndTime, playerName, playerRole, playerTeam, isNewAuction) with no explanation — the agent read it as end-to-end success and continued with the payload-consistency improvement rather than asking for details. Confidence: 0.95
- At the end of a completed plan/issue, wants a critical code analysis with a numerical rating (1–10) of robustness/quality ("dammi una valutazione da 1 a 10 della sua robustezza o qualità"), not just a completion report — the evaluation must be honest about both strengths and weaknesses (e.g., flagging vacuous tests, fragile sleeps, unhandled races) and should be grounded in actually reading the merged code. Confidence: 0.7
- When fixing a systemic bug (e.g., shadcn/Radix Select not serializing its value into FormData), expects the agent to proactively audit the whole codebase for the same pattern (`<Select name=...>`) and fix every other occurrence, not just the reported component — the agent searched and found and fixed two additional affected forms (CreateLeagueForm, AddParticipantForm). Confidence: 0.8
- Prefers destructive actions exposed as two clearly separated UI buttons with distinct roles and prominent warnings: "update" (in-season, safe, preserves rosters) vs "reset" (season start, wipes catalog AND rosters) — with an explicit warning that the reset must only be used at season start and a mandatory confirmation (typed word, e.g. "RESET", plus a confirm dialog) before it can run. Confidence: 0.85
- Before reporting test failures as introduced by the current change, expects the agent to verify they are actually new — pre-existing failures (e.g., `randomUUID is not a function` in jsdom, broken ESLint config) must be proven pre-existing via git stash and reported as out-of-scope rather than blamed on the diff. Confidence: 0.75
- Sometimes takes testing into his own hands and delegates the opposite role to the agent: "i test li faccio io tu verifica che il codice sia corretto" — when he says so, the agent should verify code correctness statically (reading the final component/action/service flow, type-check, lint) rather than writing/running test suites for him; any leftover test that fails for environment reasons (e.g., `revalidatePath` from `next/cache` unavailable in Vitest) should still be cleaned up/mocked so it doesn't block, but the verification emphasis shifts to static review. Confidence: 0.7
- When choosing among fix options for a bug, prefers the simplest, most functional one with the smallest change surface: align the client to handlers/patterns that already exist in the codebase (a 2-line client change matching what `PlayerSearchInterface` already does) rather than modifying shared server infrastructure or bypassing existing access guards — asked "quale è l'opzione più semplice e funzionale?" and confirmed "dovrebbe essere la scelta ovvia" for the client-side fix, expecting the agent to validate the choice against the code. Confidence: 0.7
- Production stack is split across two platforms: Next.js on Vercel and the Socket.IO server on Northflank (which replaced Render — an earlier assumption of Railway was wrong); the shared env values (`SOCKET_EMIT_SECRET`, `NEXT_PUBLIC_SOCKET_URL`, `ALLOWED_ORIGINS`) are loaded on both, and when the user confirms they are set correctly the agent should accept that confirmation and focus on the code fix instead of re-questioning the deployment config. Confirmed again when the user found and fixed the config error himself on the platform and reported back tersely ("esisteva un errore, ho corretto") — the agent accepted the fix and moved straight to end-to-end verification rather than re-diagnosing the deployment. Confirmed once more when the user diagnosed and fixed a stray apostrophe in `CLERK_SECRET_KEY` on Northflank himself and tersely reported the exact root cause ("CLERK_SECRET_KEY aveva un apice") along with the corrected `ALLOWED_ORIGINS` value, then pasted the overlay output — the agent accepted the fix and moved on to the next diagnostic step (service restart for env changes) instead of re-questioning the config. Confirmed again when he reported "ora è tutto giusto ma: ... Connect error: unauthorized" — he had fixed the platform config and expects the agent to accept it as correct and keep diagnosing the code/server, not re-verify the variables. Confidence: 0.85
- Requests on-demand critical review of a specific commit by hash ("analizza <hash> e dimmi cosa pensi di questo fix") and expects an honest, grounded critique of the fix — what is good (reusing already-defined handlers, symmetric cleanup) AND what is concerning (double event registration across components, missing fallback when the socket disconnects) — based on actually reading the diff plus surrounding code/handler definitions, not just a summary of what changed. Confidence: 0.6
- When asked to produce an action plan, expects the agent to first verify against the codebase that the proposed fix can reuse an existing pattern — e.g., before proposing to enrich the `auction-update` socket payload with `highestBidderName`/`autoBids`, the agent grepped the backend and confirmed the `COALESCE(lp.manager_team_name, u.username, u.id)` resolve-pattern already exists in three places — so the plan's steps are grounded in provable reuse rather than speculation. Confidence: 0.6
- Action plans should include an explicit "what NOT to do" section listing rejected alternatives and why (e.g., don't change the client handler to accept IDs instead of names, don't re-add `join-room`) — so the user can veto scope before work starts. Confidence: 0.6
- When offered the choice between a minimal backend-only fix and a full end-to-end TDD sequence (red test → backend fix → client fix → fallback refetch → cleanup), chooses the complete end-to-end option ("end-to-end") rather than the quicker partial path. Confidence: 0.7
- When the same event/payload shape is emitted from multiple code paths (e.g., `auction-created` and `auction-update` both carrying the highest bidder), expects consistency across ALL emitters: after enriching `auction-update` with `highestBidderName`, the user's paste of a raw `auction-created` event missing that field was taken by the agent as an implicit signal to enrich `auction-created` too (same `COALESCE(lp.manager_team_name, u.username, u.id)` pattern) and push, keeping client payloads uniform. Confidence: 0.55
- For displaying the highest bidder in the auction UI, prefers the team name (`manager_team_name`) over the username or raw user ID — answered "preferisco visualizzare nome squadra" when explicitly given the choice between team name and username. Confidence: 0.8
- Expects `NEXT_PUBLIC_*` env vars to be baked into the build at build time on Vercel: changing them in the dashboard without rebuilding doesn't take effect, and a missing/mis-set `NEXT_PUBLIC_SOCKET_URL` silently compiles the `localhost` default into the bundle, which the agent verifies by fetching the production JS chunks and grepping for socket URLs. The user's own verification corroborated this: he pasted the browser error page showing the client actually attempting `http://localhost:3001/` (`ERR_CONNECTION_REFUSED`), confirming the fallback URL is baked into the deployed build. When the agent finally obtained the correct Northflank URL, he validated it end-to-end before prescribing the fix: Socket.IO handshake (`EIO=4&transport=polling` → `0{"sid":...}`), 401 on `/api/emit` without secret (proof the secret is set), and CORS `access-control` headers for the Vercel origin — then concluded the server and CORS are fine and the sole fault is the missing build var. Confidence: 0.7
- When a bug persists in production (e.g., real-time socket updates still not working), explicitly asks to insert diagnostic logs and run a test to locate where the flow breaks ("Possiamo inserire dei log per capire dove è il problema? Possiamo effettuare un test per vedere se il websocket funziona?") — expects the agent to instrument the full client→server path with temporary tagged logs (e.g., `[DEBUG-]` in socket context, page handlers, and socket server) and enable an existing UI debug overlay, commit+push so he can reproduce on the deployed app (he supplies the repro context, e.g., "lega con ID16"), then report what he observes (overlay state, console, server logs) before any fix is attempted. Confirmed again: right after the instrumentation deploy he reported the deployed overlay state terse ("Overlay 'Socket Debug' ... è rosso") as the next diagnostic data point, letting the agent infer root cause (missing NEXT_PUBLIC_SOCKET_URL in the build) without further prompting. Confirmed again when, right after the URL-display instrumentation was pushed, he tersely reported the overlay state verbatim ("vedo il pallino rosso con 'ot event yet'") — the truncated text of the "No events yet" message — as the sole diagnostic input, expecting the agent to interpret it (red dot = socket not connected) and keep debugging. Confirmed once more when, after the URL-display deploy landed, he pasted the overlay's URL line verbatim ("URL: https://site--fantavega--h5y5f687z4v2.code.run:443/socket.io") plus the "No events yet" text — evidence that the env var is now baked correctly into the build, leaving the agent to continue instrumenting the still-red connection (auth/CORS) without any further prompting. Confidence: 0.95
- Considers the local `.env.local` file the source of truth for env values: when told to fetch a key from an external dashboard (Clerk API Keys), he instead quoted his existing `.env.local` line (`NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=...`) to confirm the value — the agent should proactively recognize that platform env vars can be filled by reusing existing `.env.local` values under the right naming convention (`NEXT_PUBLIC_` prefix only for frontend/browser-consumed vars on Vercel; no prefix for server-side vars on Northflank) instead of sending him on a dashboard hunt. Confidence: 0.6
- Clerk server-side token verification on the socket server (`clerkClient().authenticateRequest`) requires BOTH Clerk keys on the hosting platform (Northflank): a missing `CLERK_PUBLISHABLE_KEY` fails auth with "Publishable key is missing" even when `CLERK_SECRET_KEY` is correct — so when the socket handshake fails auth, the env checklist to give the user is `CLERK_SECRET_KEY` (no stray quotes) + `CLERK_PUBLISHABLE_KEY` + `ALLOWED_ORIGINS` (exact origin, no trailing slash), followed by a service restart for env changes to take effect. The diagnostic log line in the auth catch block is what makes this distinguishable from CORS/network failures. Confirmed again when the user sourced the missing publishable key from his local `.env.local` (`NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=...`) and the agent confirmed the exact value to add to Northflank as `CLERK_PUBLISHABLE_KEY` (no prefix). Confidence: 0.85
- On the Socket.IO server, prefers verifying the Clerk JWT directly with `verifyToken(token, { secretKey, authorizedParties })` from `@clerk/nextjs/server` over building a fake `Request("http://socket.local", { headers: { Authorization: ... } })` for `clerkClient().authenticateRequest()`: the fabricated request is fragile (never carries the real Origin, and `clerkClient()` requires both Clerk keys, causing "Publishable key is missing" on a server that only has the secret key), while `verifyToken` needs only `CLERK_SECRET_KEY` and validates `authorizedParties` against the token's own `azp` claim. The user proposed this exact approach ("è una possibile soluzione?") and the agent implemented it (including detailed auth logging of hasToken/tokenLength/origin and the `auth: success user=...` line). Confidence: 0.7
- When debugging a client-side auth/connection flow, wants to see the exact implementation rather than a summary: asked to be shown precisely how the Socket.IO client is created ("Voglio vedere precisamente come fai: const socket = io(...) e soprattutto se c'è: auth: { token: ... }") — i.e., the actual file and the exact `io()` call with its `auth` option — so he can verify the client really sends the token himself before accepting the diagnosis. Confidence: 0.55
- When cleaning up debug instrumentation after a fix is confirmed working, prefers the debug code to be commented out rather than deleted ("non rimuoverli del tutto commentali"): debug logs (`[DEBUG-...]`) and the debug overlay component/import should be preserved as comments for future debugging, not removed — while genuine error logging (e.g., the `console.error` in the auth middleware catch) stays active since it logs real failures. The agent complied by commenting out instrumentation (commit "chore(socket): comment out debug instrumentation... for future use instead of being deleted"). Confidence: 0.85
- Audits routine (non-error) production logs proactively: pastes raw Vercel log blocks and asks whether they are coherent ("i log di vercel sono coerenti?"), expecting the agent to spot suspicious recurring patterns (e.g., `POST /api/user/set-inactive` firing every ~90s) and trace them to their source in the codebase (grep for the endpoint → read the calling hook/component) to distinguish expected behavior from a bug — and to explain what triggers each pattern — rather than just confirming "everything returned 200". Confidence: 0.6
- When requesting new features, explicitly states that not breaking existing code is the top priority ("La priorità pero è quella di non rompere il codice" when asking for a delete-league button in the admin UI): feature work — especially destructive admin operations like deleting test/old leagues — must be implemented safely and preserve existing behavior, with non-regression valued over feature speed. Confidence: 0.75
- When the agent goes silent for many tool-call turns during a technical investigation (e.g., a grep rabbit hole into node_modules to check PRAGMA foreign_keys behavior), the user interrupts with a terse "cosa stai facendo?" — he expects brief progress narration and a clear explanation of what is being verified and why before code is touched, rather than long silent exploration; the agent should summarize status concisely and surface the one remaining open question. Confidence: 0.55
- For admin-panel destructive operations, prefers role-based authorization (any admin, enforced server-side via the existing `checkIsAdmin` pattern) over creator-only ownership — explicitly chose "qualsiasi admin" over "solo il creatore" for the delete-league feature; admin actions should gate on the admin role rather than on resource creator. Confidence: 0.5
- Before executing a destructive operation against production data (e.g., deleting league 15 from the admin panel), wants a pre/post verification pass first: map every table that references the entity (schema + migrations, since Turso doesn't enforce FK cascades), snapshot row counts before, run the operation on a disposable test fixture (a test league seeded with data in ALL related tables), snapshot after, and confirm both that the fixture is fully cleaned everywhere and that the real target's data stayed untouched during the test — the user proposed this pre/post idea himself ("fare un semplice test pre e post con una lega test") while being unsure of its value, and expects the agent to make it robust (all tables, not one) rather than dismiss it. Confidence: 0.7
- When writing standalone TypeScript scripts in this repo (package.json has no `"type":"module"`, so tsx compiles them as CJS), env loading must happen BEFORE the db module loads: tsx hoists static ESM imports above the code, so a static `import { db } from "../src/lib/db"` would execute before `dotenv.config({ path: ".env.local" })` and the Turso client gets built without credentials. The working pattern is `dotenv.config()` first, then `require("../src/lib/db")` (with `eslint-disable @typescript-eslint/no-require-imports`) so execution order is preserved — pinned down via incremental probes while debugging the delete-safety verification script. Confidence: 0.8
- Wants the effort of a task matched to its actual scope: when a verification request (e.g., "is the Turso DB clean for league 15?") balloons into a big script/test-suite rabbit hole, he interrupts with "credevo fosse una cosa semplice ma ci stai pensando troppo" and re-frames the ask as a quick check ("solo se è un controllo veloce però") — a simple read-only COUNT query per related table is enough; the agent should recognize when it has over-engineered, drop the elaborate tooling, and do the minimal check. Confidence: 0.65
- Fantacalcio product principle for the 1-hour response timer after being outbid: the timer must start ONLY when the user has actually seen the raise ("il timer parte solo quando l'utente vede il rilancio") — never while offline and never from presence/heartbeat signals (socket heartbeat or HTTP polling must not activate timers). Timer activation must be anchored to the user opening an auction page that shows the raise, with a client confirmation of visibility as the preferred mechanism; keeping the 120s session-staleness close is acceptable and even desirable because it keeps timers from starting for unviewed raises. Confidence: 0.85
- Fantacalcio domain rule: cooldowns are league-scoped — a user can be in cooldown for the same player in two different leagues simultaneously (abandoning an auction in Lega A blocks rebidding that player only in Lega A). Cooldown queries must filter by league when the league is known, and the batch players query must not conflate cooldowns across leagues. Confidence: 0.9
- Fantacalcio product decision: the player toggles (is_starter, is_favorite, integrity_value, has_fmv) are personal search filters, per-user and per-league — each user saves and sees only their own, so they must be stored in per-user preference tables, never written as global columns on the players table, and authorization should be "any user edits their own" rather than admin-only. Confidence: 0.85
- When updating an audit/PR status document after implementation, wants a detailed record of what was applied AND what was not applied, each with its reason ("dettagliando cosa è stato e cosa no e soprattutto il perchè") — rejected alternatives and the product rationale behind each decision must be documented in the file, not just a list of applied changes. Confidence: 0.7
- Prefers product/technical decision questions to be preceded by plain-language explanations with concrete examples ("in parole semplici", "con un esempio concreto"): after being asked to simplify, the assistant re-explained the domain (deferred response timers, socket heartbeat, N+1 cooldown queries) in simple Italian with a worked example before presenting options, and the user engaged deeply — complex concepts should be grounded in the user's actual product behavior before asking him to choose. Confidence: 0.65
- Prefers to keep a fix minimal by splitting server-only changes from client+server ones: when "start the response timer immediately if the user is online" required touching both the auction-page UI and the server, he accepted applying the base server fix now and planning the client+server part as a separate follow-up step rather than bundling both in one risky change. Confidence: 0.5
- After completing a fix, asks for a plain-language recap of what was done, why, and what would have happened if it hadn't been applied ("ora spiegami in modo semplice cosa hai fatto, perchè e cosa succedeva se non applicvamo il fix"): expects a non-technical, per-problem structure — how the feature is supposed to work → what was broken → what changed → the "senza il fix" user-facing harm (e.g., losing auctions unseen, silently losing favorites, anyone able to act as admin) — plus a brief note on how the work was validated (TDD, test counts), not a jargon-heavy change log. Confidence: 0.7
- Invokes a dedicated adversarial-review ("grilling") workflow for audit/re-audit documents and corrective patches via a custom slash command (`/greel-me` → activates the grilling skill): asked to "analizza <baseline audit> e poi valuta <re-audit doc + N patch appliers>", he expects the agent to verify every claim of the documents against the real code before judging — reading the actual routes/services, grepping for the cited patterns (e.g., `INSERT OR REPLACE`, `checkIsAdmin`), checking git history for revision drift (patches written against an older commit that no longer apply on HEAD), running the patch appliers in fail-closed `--check` dry-run, and confirming that cited evidence documents (e.g., `docs/PR/PR-*.md`) actually exist in the repo — then deliver a verdict that separates real findings from broken/unapplicable patches rather than a summary of what the documents say. Confidence: 0.6
- Grilling/evaluation sessions for audits and patches: expects the agent to read the actual code to verify an audit's claims before judging ("prima di farti domande devo capire cosa dicono l'audit e le patch, leggo i file"), confirm whether fixes are really applied on the current branch versus main (git log/diff, not just the audit's word), and deliver a verdict that clearly separates what is necessary and well-applied, what is applied differently than recommended (with the trade-offs — e.g., an opportunistic PRAGMA guard in utils.ts instead of a versioned migration: works today but lacks schema_migrations traceability), and what still needs to be done — with a concrete recommendation per item (publish branch, close the migration gap, don't touch the rest). Confidence: 0.7
- Wants audit/implementation-status documents to be self-contained so a fresh session can reconstruct the whole discussion without the original conversation: when he asks "leggendo questo file in una nuova sessione si capisce tutto? hai chiarito tutto ciò che ci siamo detti?", the agent should audit the document itself for comprehension gaps and fix them rather than reassure him. In practice this means: every finding in the severity table mapped to an explicit status (Fixed / Not applied / Open), internal contradictions in the original text resolved (e.g., the 100-vs-1000 page-cap inconsistency), the implementing commit hash cited at the top, the two vocabularies disambiguated (patch names "PR 1/2/3" vs applied "Fix 1/2/3"), and open follow-ups — including deferred product requests — listed in a dedicated section with the reason they were deferred. Confidence: 0.7
- In this repo the DB migration flow is: `database/schema.sql` is applied first (creates everything, including columns for new DBs), then the `.sql` files in `database/migrations/` are applied in alphabetical order, each tracked in a `schema_migrations` table, via `client.batch(..., 'write')`. Consequences the agent must respect when adding a migration: it must be idempotent — a plain `ALTER TABLE ... ADD COLUMN` fails on fresh DBs where `schema.sql` already creates the columns; SQLite's ALTER has no `IF NOT EXISTS`; a pure `SELECT` placeholder is rejected inside a write batch. This is why column additions that must work on both old and new DBs were handled with an opportunistic `PRAGMA table_info` guard inside `applySchemaToDb` (utils.ts) rather than as a versioned ALTER migration. Confidence: 0.7
- When told to apply an audit's missing corrections ("applica le correzioni necessarie mancanti"), expects the agent to first determine which cited fixes are already applied on the current branch (git diff/log against main, grepping for the cited patterns like `INSERT OR REPLACE`, `checkIsAdmin`, `activateTimersForUser`) and then fix only the real gaps — in this session patches A (timer viewed-only) and C (admin server actions) were already on the branch, leaving only the migration gap as actual work, with the rest verified and left untouched. Confidence: 0.6
- When an audit flags authorization holes in server actions (server actions callable directly without the admin check), expects the pattern to be applied uniformly: every action that writes admin-only data gates on `checkIsAdmin` before any query — and the agent audits sibling actions (e.g., add/remove participant rely on an `admin_creator_id` check inside their service instead, which is a different but present verification) to confirm the fix set is coherent rather than blindly adding the same guard everywhere. Confidence: 0.6
- Vitest environment config: tests under `tests/e2e/**` exercise Node APIs (`node:crypto` randomUUID, `node:child_process` execFileSync) that jsdom externalizes, so they fail under the default environment; the fix that made the whole suite pass was mapping `["tests/e2e/**", "node"]` in `environmentMatchGlobs` — when e2e tests fail for environment reasons, prefer fixing the vitest environment mapping over rewriting the tests or excluding them. Confidence: 0.6
tep rather than bundling both in one risky change. Confidence: 0.5
